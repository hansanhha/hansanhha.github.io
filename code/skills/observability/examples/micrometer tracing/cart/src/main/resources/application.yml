server:
  port: 8081

spring:
  application:
    name: cart-service

logging:
  level:
    io.opentelemetry.exporter.logging: DEBUG

management:
  tracing:
    sampling:
      probability: 1.0

  otlp:
    tracing:
      endpoint: http://localhost:4318/v1/traces